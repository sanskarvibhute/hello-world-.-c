#include <stdio.h>
int main()
{
    char n;
    printf("enter a character then I will show it's ASCII value: ");
    scanf("%c", &n);
    printf("the no. of ASCII (American Standard Code for Information Interchange) is %d", n);

    return 0;
}
